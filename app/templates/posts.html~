<table>
    {% for post in posts %}
        <tr>
            <td><img src="{{ user.avatar(50) }}" /></td>
            <td><p><i>{{ post.author.nickname }}</i> says:<br>{{ post.body }}</p></td>
        </tr>
    {% endfor %}
</table>

